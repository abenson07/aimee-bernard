# Aimee Bernard Studio

Standalone Sanity Studio for project `18kcd13f` (dataset `production`).

```sh
npm install
npm run dev
```

Studio runs at http://localhost:3333. Schema types live in `schemaTypes/`.
